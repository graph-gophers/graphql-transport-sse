# Contributing

Changes should use standard-library tests, table-driven cases, and focused commits. Run `gofmt`, `go test ./...`, `go test -race ./...`, and `go vet ./...` before submitting a pull request. Keep public APIs documented and avoid dependencies that are not required by the transport.
